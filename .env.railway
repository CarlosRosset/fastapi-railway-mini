# Configuração para Railway
# Estas variáveis devem ser configuradas no painel do Railway

# Nome do projeto
PROJECT_NAME="Template Railway FastAPI"

# URL de conexão do PostgreSQL
# No Railway, esta variável já é fornecida automaticamente quando você adiciona um PostgreSQL
# Basta referenciar a variável existente
DATABASE_URL="${{DATABASE_URL}}"

# Configurações de JWT
JWT_SECRET="${{JWT_SECRET}}"
JWT_ALGORITHM="HS256"
JWT_EXPIRATION=30

# Modo de debug (false em produção)
DEBUG=false
