# Exemplo de configuração para Railway
# Configure estas variáveis no painel do Railway (não envie este arquivo para o repositório)

# URL de conexão do PostgreSQL fornecida pelo Railway
DATABASE_URL=postgresql+asyncpg://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}

# Chave secreta para JWT (gere uma chave segura e configure no Railway)
JWT_SECRET=your-secret-key-change-in-production

# Modo de debug (recomendado false em produção)
DEBUG=false
